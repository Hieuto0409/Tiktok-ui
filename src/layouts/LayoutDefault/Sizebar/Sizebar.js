import style from './SizeBar.module.css';
import clsx from 'clsx';

function SizeBar() {
    return (
        <div className={clsx(style.container)}>
            <h2>đây là SizeBar</h2>
        </div>
    );
}

export default SizeBar;
